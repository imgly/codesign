{
  "name": "codesign",
  "version": "0.1.0",
  "description": "CoDesign: generate vector designs and print-ready PDFs via CE.SDK, over MCP. Requires Node.js >= 20 on your PATH — without it the server fails silently (see README).",
  "mcpServers": {
    "codesign": {
      "command": "npx",
      "args": ["-y", "@imgly/codesign-mcp@latest", "stdio"]
    }
  }
}
