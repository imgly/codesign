{
  "name": "codesign",
  "version": "0.1.0",
  "description": "Give your AI agent a real design engine — generate and edit designs and export print-ready PDFs over MCP, powered by CE.SDK. Requires Node.js >= 20 on your PATH — without it the server fails silently (see README).",
  "mcpServers": {
    "codesign": {
      "command": "npx",
      "args": ["-y", "@imgly/codesign-mcp@latest", "stdio"]
    }
  }
}
